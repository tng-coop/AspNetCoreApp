@page "/editor"
@rendermode InteractiveServer

@using Microsoft.JSInterop
@inject IJSRuntime JS

<h3>Quill Editor Test (v2.0.3) with Table Support</h3>

<div class="form-group">
    <label for="editor">Content</label>
    <div id="editor" style="height:200px; background-color:white;"></div>
</div>

<button class="btn btn-primary" @onclick="ShowContentSize">
    Show Content Size
</button>

@if (contentSize >= 0)
{
    <div class="mt-2">
        <strong>Content length:</strong> @contentSize characters
    </div>
}

@code {
    private int contentSize = -1;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (!firstRender)
            return;

        var loader = @"
(function() {
    var head = document.head;
    function loadCSS(href) {
        var link = document.createElement('link');
        link.rel = 'stylesheet';
        link.href = href;
        head.appendChild(link);
    }
    function loadScript(src) {
        return new Promise(function(resolve, reject) {
            var script = document.createElement('script');
            script.src = src;
            script.onload = resolve;
            script.onerror = reject;
            head.appendChild(script);
        });
    }

    // Quill core styles
    loadCSS('https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.snow.css');
    // quill-table-better styles
    loadCSS('https://cdn.jsdelivr.net/npm/quill-table-better@1.0.7/dist/quill-table-better.css');

    // Load Quill itself, then the plugin
    return loadScript('https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.min.js')
      .then(function() {
          return loadScript('https://cdn.jsdelivr.net/npm/quill-table-better@1.0.7/dist/quill-table-better.js');
      });
})()
.then(function() {
    // Register the plugin under modules/table-better
    Quill.register({'modules/table-better': QuillTableBetter}, true);

    // Initialize Quill, using toolbar.handlers to wire up the table button
    window.quill = new Quill('#editor', {
        theme: 'snow',
        modules: {
            table: false,           // disable core table
            'table-better': {},     // enable plugin
            keyboard: {
                bindings: QuillTableBetter.keyboardBindings
            },
            toolbar: {
                container: [
                    [{ header: [1, 2, 3, false] }],
                    ['bold', 'italic', 'underline', 'strike'],
                    ['blockquote', 'code-block'],
                    [{ list: 'ordered' }, { list: 'bullet' }],
                    ['link', 'image', 'video'],
                    ['table-better'],  // our table icon
                    ['clean']
                ],
                handlers: {
                    // when clicked, insert a 3×3 table
                    'table-better': function() {
                        var table = this.quill.getModule('table-better');
                        table.insertTable(3, 3);
                    }
                }
            }
        },
        placeholder: 'Type here...'
    });
})
.catch(function(err) {
    console.error('Failed to load Quill + table plugin:', err);
});
";

        await JS.InvokeVoidAsync("eval", loader);
    }

    private async Task ShowContentSize()
    {
        contentSize = await JS.InvokeAsync<int>("eval", "window.quill.getText().trim().length");
        StateHasChanged();
    }
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["BlazorWebApp.styles.css"]" />
    <ImportMap />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
</head>

<body>
    <Routes /> <!-- NO rendermode here -->

    <script>
        window.blazorCulture = {
            set: function (value) {
                localStorage.setItem("blazorCulture", value);
            },
            get: function () {
                return localStorage.getItem("blazorCulture") || "en";
            },
        };
    </script>
    <!-- 1a) Vimeo Player SDK -->
    <script src="https://player.vimeo.com/api/player.js"></script>

    <!-- 1b) your init function, globally -->
    <script>
        window.initVimeoPlayer = function (iframe) {
            const player = new Vimeo.Player(iframe);
            player.setVolume(0.5);
            player.on('play', () => console.log('▶️ Video started'));
            player.on('pause', () => console.log('⏸️ Video paused'));
            player.on('ended', () => console.log('🏁 Video ended'));
        };
    </script>

<script src="_framework/blazor.web.js"></script>
</body>

</html>
